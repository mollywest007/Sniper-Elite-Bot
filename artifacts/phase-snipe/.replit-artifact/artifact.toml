kind = "web"
previewPath = "/"
title = "Phase Snipe"
version = "1.0.0"
id = "artifacts/phase-snipe"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/phase-snipe run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/phase-snipe", "run", "build" ]
publicDir = "artifacts/phase-snipe/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
